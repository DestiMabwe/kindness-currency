import { describe, it } from 'vitest';

describe('useCouponSetBuilder', () => {
  describe('localStorage persistence', () => {
    it.todo('persists draft state to localStorage when the builder state changes');
    it.todo('rehydrates builder state from localStorage on page reload');
  });
});
